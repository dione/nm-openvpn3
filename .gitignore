# Editor / OS junk
*~
*.swp
.DS_Store

# Rust build output
/target/
**/target/

# Rendered from data/NetworkManager-VPN/nm-openvpn3-service.name.in by
# install-test.sh — the template is the source of truth.
/nm-openvpn3-service.name

# coderabbit local review file (contains workingDirectory PII)
/coderabbit-review.jsonl

# Fork-local working notes — not part of the upstream tree
/reference/
/docs/superpowers/
