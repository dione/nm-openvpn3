# No vendor/ tree in the dh-cargo layout — Rust deps come from
# /usr/share/cargo/registry via librust-*-dev.  extend-diff-ignore
# only covers fork-local working trees + cargo build output.
extend-diff-ignore = "(^(target|reference|docs/superpowers)/|^coderabbit-review\.jsonl$|^po/.*\.mo$)"
